// 编写好规则
export const rules_phone = {
  // name: [{规则1}, {规则2}, {规则3}]
  num: [
    {
      // pattern: /^(13[0-9]|14[01456879]|15[0-35-9]|16[2567]|17[0-8]|18[0-9]|19[0-35-9])\d{8}$/, // 手机号验证规则
      pattern: /^\d{11}$/, // 手机号验证规则
      message: '请输入正确的手机号',
      trigger: 'blur'
    }
  ],
  code: [
    {
      pattern: /^[0-9]{6}$/, // 由小写字母和数字 3位以上 组成
      message: '请输入正确的验证码',
      trigger: 'blur'
    }
  ]
}
